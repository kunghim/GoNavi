# 向量与缓存数据源

> scan_id: `incremental-20260811-46d3afb3`
> head_commit: `46d3afb36e8b04c1931ccad03880d5f5d9390d5f`

> 本文件是 gonavi-scan 功能地图的领域分册。能力 ID 保持稳定；扫描时只更新受影响领域。

| ID | 数据源与接入模式 | 典型用户任务 | 已知边界 | 状态 | 证据 |
|---|---|---|---|---|---|
| CAP-DS-REDIS | Redis，内置 | DB/Key、多类型编辑、命令、监控和导入导出 | Cluster/Sentinel 与 SSH 组合后端不支持，但连接弹窗尚未提前门控 | 部分实现 | `methods_redis.go`；`internal/redis`；`ConnectionModalNetworkSecuritySection.tsx` |
| CAP-DS-CHROMA | Chroma，内置 | Collection、向量检索和元数据过滤 | 字段元数据会读取文档、元数据和向量；简化 SQL 会静默忽略 WHERE | 部分实现 | `chroma_impl.go` |
| CAP-DS-QDRANT | Qdrant，内置 | Collection、count/scroll/search 和 Payload 过滤 | 字段元数据会读取 Payload 和向量；简化 SQL 会静默忽略 WHERE | 部分实现 | `qdrant_impl.go` |
| CAP-DS-MILVUS | Milvus，内置 | Collection、向量搜索和标量过滤 | Schema 与维度受服务端约束 | 已实现 | `milvus_impl.go`；`milvus_impl_test.go` |

### 向量数据源细分

| ID | 具体能力 | 用户入口与操作 | 边界/异常 | 状态 | 证据 |
|---|---|---|---|---|---|
| CAP-VECTOR-A01 | 浏览 Collection 和 Schema | 向量连接树 → Collection → 元数据 | Chroma/Qdrant 字段展开会读取最多 20 条真实记录并包含向量正文；Milvus 使用服务端 Schema | 部分实现 | `chroma_impl.go:254-280`；`qdrant_impl.go:244-269`；`milvus_impl.go` |
| CAP-VECTOR-A02 | 向量检索和标量/元数据过滤 | 查询标签 → 专用查询/JSON → 执行 | JSON 过滤路径可用；Chroma/Qdrant 简化 SQL 接受但不执行 WHERE | 部分实现 | `chroma_impl.go:848-884`；`qdrant_impl.go:815-850`；`milvus_impl.go` |
| CAP-VECTOR-A03 | Chroma Collection 预览与 JSON 命令 | Chroma 查询 → list/get/query → 查看文档和 Metadata | 写命令与查询命令分离；include 决定是否返回向量 | 部分实现 | `chroma_impl.go` 的 queryJSON/getCollectionRows/queryCollection |
| CAP-VECTOR-A04 | Qdrant Point 滚动、搜索与 Payload 操作 | Qdrant 查询 → scroll/search/count 或 JSON 命令 | 动作白名单与 wait 语义按服务端版本处理 | 部分实现 | `qdrant_impl.go` 的 queryJSON/scrollPoints/searchPoints |
| CAP-VECTOR-A05 | Milvus Schema、查询与向量搜索 | Milvus 连接树/查询 → Collection → 执行 | 数据库枚举失败会回退配置库；过滤表达式遵循 Milvus 语法 | 已实现 | `milvus_impl.go`；`milvus_impl_test.go` |
